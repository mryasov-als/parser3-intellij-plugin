# ---------------------------------------------------------------------------
# 1. КОММЕНТАРИИ
# ---------------------------------------------------------------------------

# 1.1 Однострочный комментарий (# в колонке 0)
# это комментарий LINE_COMMENT

# 1.2 Блочный комментарий ^rem{}
^rem{это блочный комментарий BLOCK_COMMENT}

# 1.3 Блочный комментарий с вложенными скобками
^rem{
	уровень 1 {
		уровень 2 {
			уровень 3
		}
	}
}

# 1.4 Комментарий внутри круглых скобок (выражения)
^if(
	$a eq 'b'  # это комментарий внутри выражения
	&& $c
){ok}

# 1.5 Экранированные скобки внутри ^rem
^rem{
	экранированная скобка: ^{
	экранированная скобка: ^}
}

# ---------------------------------------------------------------------------
# 2. ОПРЕДЕЛЕНИЯ МЕТОДОВ (@method)
# ---------------------------------------------------------------------------

@CLASS
test

@USE
main.p

@BASE
test

@OPTIONS
locals



# 2.1 Простой метод
@simple_method[]

# 2.2 Метод с параметрами
@method_with_params[param1;param2;param3]

# 2.3 Метод с локальными переменными
@method_with_locals[param1][local1;local2]

# 2.4 Метод с *параметром (передача по ссылке)
@method_with_ref[*ref_param;normal_param]

# 2.5 Статический метод
@static:method_name[]


# 2.6 Специальные методы
@auto[]
@conf[]
@main[]
@GET_property[]
@SET_property[]

# 2.7 Директивы
@CLASS
ClassName

@BASE
BaseClassName

@USE
/path/to/file.p
relative/path.p

@OPTIONS
locals
other
partial
dynamic
static


@method[]

# ---------------------------------------------------------------------------
# 3. ВЫЗОВЫ МЕТОДОВ (^method)
# ---------------------------------------------------------------------------

# 3.1 Простой вызов
^method[]

# 3.2 Вызов с параметрами
^method[param1]
^method[param1;param2]

# 3.3 Цепочка методов
^object.method1[].method2[]

# 3.4 Вызов статического метода класса
^ClassName:staticMethod[]

# 3.5 Вызов конструктора
^ClassName::create[]

# 3.6 Важные методы
^MAIN:method[]
^BASE:method[]
^self.method[]
^result.method[]

# 3.7 Ключевые слова (keywords)
^if($condition){true}{false}
^while($i < 10){^i.inc[]}
^for[i](1;10){$i}
^switch[$var]{^case[a]{A}^case[b]{B}}
^try{risky}{^throw[error]}
^eval($expression)
^break[]
^continue[]
^return[value]
^sleep[1000]
^use[file.p]
^process{text}
^cache[60]{cached content}
^connect[$connection]{^void:sql{SELECT 1}}
^method(123)
^method($x)

# ---------------------------------------------------------------------------
# 4. ПЕРЕМЕННЫЕ ($var)
# ---------------------------------------------------------------------------

# 4.1 Простая переменная
$variable

# 4.2 Переменная с присваиванием
$var[value]

# 4.3 Цепочка переменных
$object.property.subproperty

# 4.4 Анонимная переменная (хеш-поле)
$.field[value]

# 4.5 Важные переменные
$result
$self
$MAIN
$BASE

# 4.6 Доступ через MAIN/BASE
$MAIN:variable
$BASE:variable

# 4.7 Переменная с индексом
$array[0]
$hash[key]
$var.method[]

# 4.8 Вложенные структуры
$data[
	$.x[1]
	$.y[2]
	$.inner[
		$.a[3]
		$.b[4]
	]
]

# ---------------------------------------------------------------------------
# 5. СКОБКИ И РАЗДЕЛИТЕЛИ
# ---------------------------------------------------------------------------

# 5.1 Квадратные скобки
^method[content]
$var[value]

# 5.2 Фигурные скобки
^if($a){true}{false}

# 5.3 Круглые скобки (только в выражениях)
^if($a && $b){ok}

# 5.4 Точка с запятой (разделитель)
^method[a;b;c]
^if($a){x;y}

# 5.5 Экранированные скобки
^{literal brace}
^[literal bracket]
^;literal semicolon

# ---------------------------------------------------------------------------
# 6. ВЫРАЖЕНИЯ (внутри круглых скобок)
# ---------------------------------------------------------------------------

# 6.1 Операторы сравнения
^if($a == $b){equal}
^if($a != $b){not equal}
^if($a < $b){less}
^if($a > $b){greater}
^if($a <= $b){less or equal}
^if($a >= $b){greater or equal}

# 6.2 Логические операторы
^if($a && $b){and}
^if($a || $b){or}
^if(!$a){not}
^if(!||$a){not or}

# 6.3 Словесные операторы
^if($a eq 'string'){eq}
^if($a ne 'string'){ne}
^if($a lt 'string'){lt}
^if($a gt 'string'){gt}
^if($a le 'string'){le}
^if($a ge 'string'){ge}
^if(def $a){defined}
^if($a is string){is type}
^if(-f '/path/file'){file exists}
^if(-d '/path/dir'){directory exists}

# 6.4 Арифметические операторы
^eval($a + $b)
^eval($a - $b)
^eval($a * $b)
^eval($a / $b)
^eval($a % $b)

# 6.5 Строки в выражениях
^if($s eq 'single quoted'){ok}
^if($s eq "double quoted"){ok}

# 6.6 Числа в выражениях
^if($a > 10){ok}
^if($a == 123){ok}

# 6.7 Булевы значения
^if(true){yes}
^if(false){no}

# ---------------------------------------------------------------------------
# 7. SQL БЛОКИ
# ---------------------------------------------------------------------------

# 7.1 Простой SQL
^void:sql{
	SELECT * FROM users
}

# 7.2 SQL с переменными
^void:sql{
	SELECT * FROM users WHERE id = $id
}

# 7.3 SQL с вызовами методов
^void:sql{
	UPDATE users SET name = '^form:name.trim[]' WHERE id = $id
}

# 7.4 SQL с директивами
^void:sql{
	SELECT * FROM users
	^if($name){
		WHERE name = '$name'
	}
}

# 7.5 Вложенный SQL
$list[^table::sql{
	SELECT id FROM users WHERE parent_id = ^int:sql{SELECT id FROM parents LIMIT 1}
}]

# 7.6 Разные типы SQL-конструкторов
^table::sql{SELECT * FROM t}
^string::sql{SELECT name FROM t}
^hash::sql{SELECT * FROM t}[$.limit[1]]
^int:sql{SELECT COUNT(*) FROM t}
^double:sql{SELECT AVG(price) FROM t}
^void:sql{DELETE FROM t}

# 7.7 SQL с foreach
^void:sql{
	WHERE id IN (^data.foreach[k;v]{$v.id}[,])
}

# 7.8 SQL с switch/case
^void:sql{
	^switch[$type]{
		^case[a]{field = 'A'}
		^case[b]{field = 'B'}
	}
}

# ---------------------------------------------------------------------------
# 8. ТОЧКИ И ДВОЕТОЧИЯ
# ---------------------------------------------------------------------------

# 8.1 Точка в цепочке методов
^object.method1[].method2[]

# 8.2 Точка в переменных
$var.field.subfield

# 8.3 Одно двоеточие (статический метод)
^Class:method[]

# 8.4 Двойное двоеточие (конструктор)
^Class::create[]

# 8.5 Хеш-поля
$.field[value]
$data[$.x[1]$.y[2]]

# ---------------------------------------------------------------------------
# 9. ДИРЕКТИВЫ @USE / ^use
# ---------------------------------------------------------------------------

# 9.1 Директива @USE
@USE
/absolute/path/to/file.p
relative/path/to/file.p
../parent/file.p

@method[]
# 9.2 Метод ^use
^use[/path/to/file.p]
^use[relative.p]

# ---------------------------------------------------------------------------
# 10. СПЕЦИАЛЬНЫЕ СЛУЧАИ
# ---------------------------------------------------------------------------

# 10.1 Экранирование ^
^^not_a_method
^^^method[]

^$not_var
^^$var

# 10.2 Экранирование $
^$not_a_variable

# 10.3 Пустые скобки
^method[]
$var[]

# 10.4 Многострочные конструкции
^if(
	$a
	&& $b
	&& $c
){
	многострочное
	тело
}

# 10.5 Хеш с вложенной структурой
$hash[^hash::create[
	$.key1[value1]
	$.key2[value2]
	$.nested[^hash::create[
		$.inner[value]
	]]
]]

# 10.6 Переменная с динамическим именем
${dynamic_name}
$prefix${suffix}

# 10.7 Комментарий НЕ в колонке 0 (это НЕ комментарий!)
	# это не комментарий, это текст с решёткой

# 10.8 Кавычки в SQL (не Parser3 строки)
^void:sql{
	SELECT * FROM users WHERE name = 'John' AND status = "active"
}

# 10.9 Методы foreach/for/select с особым синтаксисом
^data.foreach[key;value]{$key: $value}
^for[i](1;10){$i}
^data.sort{$a.name}

# 10.10 Конструкторы разных типов
^hash::create[]
^table::load[file.txt]
^array::copy[$source]
^string::base64[$data]

# ---------------------------------------------------------------------------
# 11. ПЕРЕМЕННЫЕ С ТЕЛОМ {} И ВЫРАЖЕНИЯМИ ()
# ---------------------------------------------------------------------------

# 11.1 Переменная с телом {}
$var{тело переменной}
$var{$inner_var и текст}

# 11.2 Переменная с вложенным выражением
$var{$var2(123)}
$var{$var2($a + $b)}

# 11.3 Переменная с телом и методом внутри
$var{^method[param]}
$var{^if($a){yes}{no}}

# ---------------------------------------------------------------------------
# 12. МНОГОМЕРНЫЕ ХЕШИ
# ---------------------------------------------------------------------------

# 12.1 Двумерный хеш
$hash[key1][key2]

# 12.2 Трёхмерный хеш
$data[level1][level2][level3]

# 12.3 Хеш с переменным ключом
$hash[$key]
$hash[$obj.field]

# 12.4 Вложенные хеши с разными скобками
$data[$key]{тело}
$hash[key1][key2]{содержимое}

# 12.5 Создание многомерного хеша
$matrix[^hash::create[
	$.row1[^hash::create[
		$.col1[1]
		$.col2[2]
	]]
	$.row2[^hash::create[
		$.col1[3]
		$.col2[4]
	]]
	$.row3[
		$.code{
			$var[123]
			$var(123)
		}
	]
]]

# ---------------------------------------------------------------------------
# 13. СЛОЖНЫЕ ВЫРАЖЕНИЯ В СКОБКАХ
# ---------------------------------------------------------------------------

# 13.1 Вложенные круглые скобки
^if(($a + $b) * $c > 10){ok}
^if(($a && $b) || ($c && $d)){ok}

# 13.2 Скобки в скобках с операторами
^eval(($x + $y) / ($z - $w))
^if((($a > 0) && ($b < 10)) || $c){ok}

# 13.3 Вызов метода внутри выражения
^if($obj.method[] > 0){ok}
^if(^math:abs[$val] < 10){ok}

# 13.4 Строки внутри вложенных скобок
^if(($a eq 'test') && ($b ne "value")){ok}

# 13.5 Числа и булевы во вложенных скобках
^if((true && $a) || (false || $b)){ok}
^if(($x > 100) && ($y < 200)){ok}

# 13.6 Сложные комбинации
^if(
	(def $a && ^a.length[] > 0)
	|| (def $b && $b ne 'empty')
	|| ($c is hash && $c.count[] >= 5)
){
	сложное условие выполнено
}

# ---------------------------------------------------------------------------
# 14. ЦЕПОЧКИ ПЕРЕМЕННЫХ И МЕТОДОВ С ВЫРАЖЕНИЯМИ
# ---------------------------------------------------------------------------

# 14.1 Цепочка переменных с выражением в конце
$var.x.y.z($a + $b)
$obj.field.subfield($condition)

# 14.2 Цепочка методов с выражением
^method.x.y.z($a && $b)
^obj.chain1.chain2($param)

# 14.3 Смешанные цепочки
$data.items.filter($x > 0)
^list.sort.reverse($a < $b)

# 14.4 Цепочка с несколькими скобками
$var.method1[].method2($a).field
^obj.get[key].process($x).result[]

# 14.5 Длинные цепочки
$a.b.c.d.e.f($condition)
^x.y.z.w.v.u[param]

# ---------------------------------------------------------------------------
# 15. КОМБИНАЦИИ ВСЕГО
# ---------------------------------------------------------------------------

# 15.1 Переменная с хешем, телом и выражением
$result[^hash::create[
	$.data{$var($a + $b)}
	$.items[$list.filter($x > 0)]
]]

# 15.2 Метод с вложенными структурами
^process{
	$hash[
		$.x{^if($a){$b}{$c}}
		$.y($condition && true)
	]
}

# 15.3 SQL с вложенными выражениями
^void:sql{
	SELECT * FROM users
	WHERE id IN (^list.foreach[;v]{$v.id}[,])
	AND status = ^if($active){'active'}{'inactive'}
	AND age > ^eval($min_age + 5)
}

# 15.4 Глубоко вложенные скобки
^if(
	(
		($a > 0) && (
			($b < 10) || ($c == 5)
		)
	) || (
		def $d && $d.valid[]
	)
){
	^if($a){
		ok
	}
}

# теги
<form>
	$html
</form>

^if($a){
	<div class="main">
		$var[text]
		<title>$var</title>
		<ul>
			^for[i](1;10){
				<li class="line-$i">text_$i</li>
			}
		</ul>
	</div>
}

# всякие скобки
^if($a eq 'b)'){
	field = 'ok'
}($d eq "x)"){
	field = 'aaa'
}($e eq 'f'){
	field = 'bbb'
}($a eq ^)){
	$var{
		<div>
	}
}($a eq 'b)'){
	field = 'ok'
}($d eq "x)"){
	field = 'aaa'
}($e eq 'f'){
	field = 'bbb'
}($a eq ^)){
	x
}($f eq "x^)"){
	z
}($f eq 'x^''){
	z
}($z eq '^method[]'){
	^method[]
}

$calendar[^date:calendar[rus]($year;$month;1)]


# ---------------------------------------------------------------------------
# 16. ДОПОЛНИТЕЛЬНЫЕ ТЕСТ-КЕЙСЫ
# ---------------------------------------------------------------------------

# 16.1 Экранирование кавычек внутри строк
^if($a eq 'x^'y'){одинарная с экранированной кавычкой}
^if($a eq "x^"y"){двойная с экранированной кавычкой}

# 16.2 ^^ внутри выражения
^if($a eq '^^'){символ каретки}
^if($a eq "^^"){символ каретки в двойных}

# 16.3 Hex-коды
текст^#20с^#20пробелами
^#0Aперевод строки
^#09табуляция

# 16.4 Отрицательные числа в выражениях
^if($a > -10){отрицательное}
^if($a < -100){большое отрицательное}
^eval($a + -5)
^eval(-10 * $b)

# 16.5 Числа с плавающей точкой
^if($a > 3.14){пи}
^if($a < 0.001){малое}
^eval($a * 2.5)
^eval(0.5 + 1.5 + 999)
$var(.05)




# 16.6 @auto[] в начале файла (эмуляция — уже есть @CLASS выше)
# Реальный тест: отдельный файл начинающийся с @auto[]

# 16.7 Несколько @CLASS в одном файле
@CLASS
FirstClass

@create[]
$self.name[first]

@CLASS
SecondClass

@BASE
FirstClass

@create[]
^BASE:create[]
$self.type[second]

# 16.8 ^rem с разными скобками
^rem[комментарий в квадратных скобках]
^rem(комментарий в круглых скобках)
^rem{комментарий в фигурных скобках}

# 16.9 Вложенный ^if внутри ^if
^if($a){^if($b){оба истинны}{только a}}
^if($x > 0){
	^if($y > 0){
		первый квадрант
	}{
		четвёртый квадрант
	}
}{
	^if($y > 0){
		второй квадрант
	}{
		третий квадрант
	}
}

# 16.10 Три формы переменных: [] () {}
$var[значение в квадратных]
$var(выражение)
$var{тело в фигурных}
$var[$key]
$var($a + $b)
$var{^method[]}

# 16.11 Комбинации скобок
$hash[key]{тело}
$data[$index](выражение)
$obj.method[param]{тело}

# 16.12 Пустой первый параметр в foreach
^data.foreach[;v]{$v.id}
^hash.foreach[k;]{$k}

# 16.13 Строки с пробелами (регрессия)
^if($s eq 'hello world'){с пробелом}
^if($s eq "hello world"){с пробелом в двойных}
^if($s eq 'a b c d e'){много пробелов}

# 16.14 Сложные экранирования в строках
^if($a eq '^;^## ^{^}'){все спецсимволы}
^if($a eq "^$var не переменная"){экранированный доллар}
^if($a eq '^$var не переменная'){экранированный доллар}

# 16.15 Методы без скобок после цепочки (ошибочный синтаксис, но лексер должен обработать)
$obj.field
^method.chain

# 16.16 Операторы in/!in
^if($a in 'abc'){содержит}
^if($a !in 'xyz'){не содержит}
^if(in '/dir/'){in}
^if(-f 'file.p' || in '/dir/' || is 's^}tring'){}
^if(-f "file.p" || in "/dir/" || is "s^}tring"){}

# 16.17 Тернарный через ^if в одну строку
$result[^if($condition){да}{нет}]

# 16.18 SQL с комментариями
^void:sql{
	-- SQL комментарий
	SELECT * FROM users
	/* многострочный
	   SQL комментарий */
	WHERE id = $id
}

# 16.19 Регулярные выражения (если поддерживаются)
^if($s ~ '/pattern/'){regex match}

# 16.20 Присваивание с выражением
$var($a + $b * $c)
$result(^method[] + 10)


# ---------------------------------------------------------------------------
# 16. ДОПОЛНИТЕЛЬНЫЕ ОПЕРАТОРЫ
# ---------------------------------------------------------------------------

# 16.1 Побитовая инверсия (~)
^if(~$flags & 0x01){побитовая инверсия}

# 16.2 Целочисленное деление (\)
$quotient(10 \ 3)
^if($total \ $count > 5){целочисленное деление}

# 16.3 Все битовые операторы вместе
$result($a & $b | $c !| $d)
$shifted($val << 2)
$shifted2($val >> 3)

# ---------------------------------------------------------------------------
# 17. TAINT/UNTAINT ОПЕРАТОРЫ
# ---------------------------------------------------------------------------

# 17.1 ^taint без типа
^taint[$user_input]

# 17.2 ^taint с типом преобразования
^taint[as-is][$body]
^taint[html][$content]
^taint[uri][$url]
^taint[sql][$query]
^taint[optimized-as-is][$text]
^taint[optimized-html][$html]
^taint[file][$filename]
^taint[regex][$pattern]
^taint[js][$javascript]
^taint[json][$data]

# 17.3 ^untaint
^untaint{$tainted_data}
^untaint[html]{$content}
^untaint[optimized-html]{всё содержимое}

# 17.4 ^apply-taint
^apply-taint[$text]
^apply-taint[uri][$url]
^apply-taint[html][^taint[as-is][$body]]

# ---------------------------------------------------------------------------
# 18. @OPTIONS ДИРЕКТИВА
# ---------------------------------------------------------------------------

@OPTIONS
locals
partial
dynamic

@OPTIONS
static

# ---------------------------------------------------------------------------
# 19. CALLER И REFLECTION
# ---------------------------------------------------------------------------

# 19.1 $caller - системная переменная контекста
$caller.self
$caller.method
$caller.method.name
$caller.$name
^caller.$name.inc[$step]

# 19.2 ^reflection - статические методы
^reflection:class[]
^reflection:class[$object]
^reflection:classes[]
^reflection:copy[$object]
^reflection:create[$class_name;$constructor_name;$params]
^reflection:method[$object;method_name]
^reflection:methods[$object]
^reflection:dynamical[]
^reflection:dynamical[$class]
^reflection:is[$object;$class_name]
^reflection:fields[$object]

# ---------------------------------------------------------------------------
# 20. ЧИСЛА С ПЛАВАЮЩЕЙ ТОЧКОЙ
# ---------------------------------------------------------------------------

# 20.1 Обычные float
$pi(3.14159)
$small(0.001)
$big(123456.789)

# 20.2 Числа начинающиеся с точки
$half(.5)
$tiny(.001)
^if($value > .25){больше четверти}

# 20.3 Float в выражениях
^if($x > 3.14 && $y < .5){ok}
$result($a * 2.5 + .1)

# ---------------------------------------------------------------------------
# 21. ^rem[] И ^rem() - НЕ КОММЕНТАРИИ
# ---------------------------------------------------------------------------

# 21.1 ^rem с квадратными скобками - это метод
^rem[это не комментарий, а вызов метода rem]

# 21.2 ^rem с круглыми скобками - это тоже метод
^rem(это выражение внутри скобок)

# 21.3 Только ^rem{} - настоящий комментарий
^rem{это настоящий комментарий BLOCK_COMMENT}


# еще методы
^if($z eq '^method[] string'){
	это метод
}($z eq "string $var"){
	это переменная
}($z eq "asdf"){

}($z eq 'asdf'){

}